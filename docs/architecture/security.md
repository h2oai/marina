# Security & API — Authentication, Rate Limits, HTTP Hardening, Gateway

**When to read this:** you are adding an HTTP route, an upload or asset path, an MCP transport change, a login path, rate limiting, external auth, SSRF-sensitive fetches, or federation. `CLAUDE.md` → "Security & API" states the invariants; this page is the complete reference including the 2026-09-22 HTTP hardening pass.

## Authentication and rate limiting
- **API authentication**: HTTP endpoints require `MODEL_API_KEYS` / `MEM_API_KEYS` or explicit `MARINA_OPEN_API=true` for dev mode. The dashboard/asset/canvas API auth gate (`authenticateRequest` in `src/net/auth-middleware.ts`) also honors `MARINA_OPEN_API=true`: a missing/invalid token is allowed through as the `OPEN_API_ENTITY_ID` sentinel (valid tokens still resolve to their real entity). Dev-only — never enable in production.
- **Rate limiting**: all endpoints rate-limited — WebSocket commands (5/sec), MCP tools (5/sec), Model API (2/sec per IP), Memory API (10/sec per agent)
- **Login limits**: `MARINA_MAX_LOGINS` caps concurrent entity-bound connections instance-wide (0 = unlimited); `MARINA_LOGIN_ATTEMPTS_PER_MIN` throttles login/reconnect attempts per IP (default 10, 0 = off). Enforced centrally in `engine.login()`/`reconnect()`; internal room/crew agents authenticate via the internal token (`internalToken` on the login/auth WS message) and are exempt from both
- **Account-link codes** (`src/engine/commands/link.ts`): `link` issues a 6-symbol CSPRNG code (5-minute expiry) that Telegram/Discord redeem via `redeemLinkCode` — a binding only; the login that follows still goes through `engine.login()`. Guessing is limited per external account (5 code-shaped attempts, +1 per 2 min) and globally on failures (30, +1 per 10 s); both buckets set `ignoreBypass`, so the `local` profile's `RateLimiter.bypass` never relaxes them. A limited attempt is not checked and falls through like ordinary text, so name login is never blocked.
- **Optional external auth (better-auth)**: OFF by default. `MARINA_AUTH=better-auth` enables email/password + social OAuth sign-in on the web surfaces and gates passwordless name-login (`engine.config.authRequired`). A verified identity is bridged to a *named* entity (the artilect stays the in-world identity); agents keep token auth. Admins by verified email (`MARINA_AUTH_ADMIN_EMAILS`), not name. Lazy-loaded provider in `src/auth/better-auth-provider.ts`, bridge in `src/net/auth-api.ts` (`/api/auth-status`, `/api/auth/*`, `POST /api/auth-session`), dashboard gate in `dashboard/src/components/AuthGate.tsx`. When off: no import, no routes, no schema effect. Setup: [docs/authentication.md](docs/authentication.md)

## HTTP hardening
- **HTTP hardening (2026-09-22)** (`src/net/http-utils.ts`): `clientIp(req, peer)` uses the socket peer unless `MARINA_TRUST_PROXY=true` and feeds EVERY per-IP limiter; named limiters `HTTP_RATE_LIMITS` (`dashboard` 60/10 s per principal, `mutation` 30/10 s for canvas/asset writes, `publicRead` 30/10 s for `/api/entity/*`, `mcpSession` 10/min per IP for MCP session/login/auth). `refuseOpenApiWrite()` — the `OPEN_API_ENTITY_ID` sentinel is read-only on asset and canvas routes too (403), except under the `local` trust profile (`isLocalUngated()`): with `MARINA_OPEN_API=true` on a loopback bind and no auth, the browser IS the single operator, so the Canvas dev write flow stays open. The Playwright canvas suite (`dashboard/e2e/canvas-server.ts`, which boots with `MARINA_OPEN_API=true`) exercises exactly this path; shared/public keep the sentinel read-only. Assets: MIME allowlist + magic-byte check (`normalizeAssetMime`), off-list → `application/octet-stream`; every asset response carries `nosniff`, `Content-Security-Policy: default-src 'none'; sandbox` (PDF: no `sandbox`, stays inline for the iframe viewers), `Content-Disposition: attachment` for anything not raster image/audio/video/PDF. HTML surfaces get `nosniff`, `Referrer-Policy`, `X-Frame-Options: SAMEORIGIN`, `frame-ancestors 'self'` (no script-src CSP — the built dashboard needs inline styles/blob URLs). `Bun.serve` has an `error()` handler and `maxRequestBodySize` = max(`MARINA_MAX_REQUEST_BODY_BYTES` 8 MiB, `MARINA_MAX_UPLOAD_BYTES` 50 MiB). MCP transport: bearer required when `MODEL_API_KEYS`/`MARINA_AUTH` is set or the bind is non-loopback; `enableDnsRebindingProtection` + `MARINA_MCP_ALLOWED_HOSTS`; `quoteArg` REJECTS whitespace/control chars in interpolated params (the command tokenizer has no quote grammar); the `flywheel` tool routes through `runCmd` (`code sandbox …`) so gates apply. Internal model token is 32 random bytes. `maskKey` shows last-4 only.

## SSRF, gateway, dashboard scoping, adapters
- **SSRF protection**: `src/net/url-guard.ts` blocks private IPs, IPv6 loopback, link-local, cloud metadata, IPv4-mapped IPv6
- **Gateway auth**: optional `GATEWAY_SECRET` env var authenticates the gateway *handshake* (who may present as a `Gateway_`-named peer). It does **not** by itself fence bridged-channel participation or cross-instance tells — those are rank-0 commands open to any logged-in entity, so with passwordless name-login (default) a remote peer can still act as an ordinary local entity. For a hard federation boundary, also enable `MARINA_AUTH=better-auth` and don't expose open login.
- **Gateway protocol**: structured JSON fields with version number, regex fallback for backward compatibility
- **Dashboard scoping**: broadcasts filter sensitive data (connectionId, raw command input stripped)
- **Adapter persistence**: Discord/Telegram user mappings persisted to DB, auto-reconnect on restart

See also: `docs/authentication.md`, `docs/guides/identity.md`, `docs/guides/deployment.md`, `SECURITY.md`; `docs/architecture/civic-substrate.md` (gates, trust profile, exec approval).

## Dashboard Content-Security-Policy (2026-09-22)

Every HTML route (`/dashboard`, `/canvas`, `/who*`, `/chat`, `/ask`, the not-built placeholder) carries `HTML_CSP` from `http-utils.ts`: `default-src 'self'; script-src 'self' https://cdnjs.cloudflare.com/ajax/libs/pdf.js/; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; img-src 'self' data: blob:; media-src 'self' blob: data:; font-src 'self' data: https://fonts.gstatic.com; connect-src 'self' ws: wss:; worker-src 'self' blob:; frame-src 'self' https:; frame-ancestors 'self'; object-src 'none'; base-uri 'self'; form-action 'self'`. Grants beyond `'self'` exist for: Google Fonts (`index.html`, `unified-canvas.css`), the pdf.js worker on cdnjs (`PdfNode.tsx`), and operator-supplied `embed` iframes (`CanvasNodeEmbed.tsx`, `AssetLightbox.tsx`, sandboxed). `/chat` and `/ask` have inline scripts and get per-file `'sha256-…'` hashes appended — never `'unsafe-inline'` for scripts. `MARINA_DASHBOARD_CSP=off` drops the header; any other value is sent verbatim. Verified: 0 CSP console violations on all seven routes under Playwright.

## Ops API

`GET /api/ops/overview` and `POST /api/ops/agents/:name/stop` live in `src/net/ops-api.ts` (contract `src/net/ops-types.ts`, re-exported type-only by `dashboard/src/lib/ops-types.ts` with a contract test). Scoped like the memory observability API: privileged principals see every agent; a resident sees its own transitive spawn lineage. The stop route is privileged (`authorizePrivileged(…, "agent.spawn")`) and cascades children-first, emitting one `agent_stop` event per agent. `mcpAuthRequired` and `loopbackBind` are computed by local mirrors of the mcp-server / websocket-server predicates (to avoid an import cycle) — change both together.
